function maxLength (nr){
    // this.value=this.value.replace(/[^0-9]/g,'');
    // console.log("max");
    switch (nr) {
        case 2:
            document.getElementById(digitTwo).focus();
            break;
    
        default:
            break;
    }
}
